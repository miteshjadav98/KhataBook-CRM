{
  "name": "khatabook-crm-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for KhataBook CRM containing both backend and frontend",
  "scripts": {
    "start": "concurrently \"npm run start:api\" \"npm run start:frontend\"",
    "start:api": "npm run start:dev --prefix khatabook-api",
    "start:frontend": "npm run dev --prefix khatabook-frontend",
    "install:all": "npm install && npm install --prefix khatabook-api && npm install --prefix khatabook-frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
